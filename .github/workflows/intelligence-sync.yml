name: Refresh intelligence sources (paused)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Cloud classification is disabled. The Mac building collector is the sole publisher. Legacy workflow retained in docs/archive/intelligence-sync.yml.txt.'
