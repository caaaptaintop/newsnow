export function sourceAdminPage(email: string) {
  const safeEmail = email.replace(/[&<>"']/g, char => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[char]!)
  return `<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>信息源管理中心</title>
<style>
:root{font-family:Inter,"PingFang SC","Microsoft YaHei",sans-serif;color:#172033;background:#f4f6f9;font-synthesis:none}*{box-sizing:border-box}body{margin:0;min-width:1024px}.shell{display:grid;grid-template-columns:230px 1fr;min-height:100vh}.side{background:#111827;color:#d7deea;padding:24px 16px;position:sticky;top:0;height:100vh}.brand{display:flex;align-items:center;gap:10px;color:#fff;font-weight:700;font-size:18px;padding:0 10px 24px}.brand svg{width:24px}.side h2{font-size:12px;color:#8792a5;font-weight:600;letter-spacing:.08em;margin:10px}.topic{width:100%;border:0;background:transparent;color:inherit;border-radius:9px;padding:12px 11px;margin:3px 0;text-align:left;display:flex;justify-content:space-between;cursor:pointer;font-size:14px}.topic:hover,.topic.active{background:#273449;color:#fff}.topic small{background:#3a4658;border-radius:12px;padding:2px 8px}.side-foot{position:absolute;bottom:20px;left:25px;right:20px;color:#9aa5b5;font-size:12px;line-height:1.6}.main{padding:28px 34px}.top{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:20px}.top h1{margin:0 0 6px;font-size:25px}.muted{color:#6c7789;font-size:13px}.summary{display:flex;gap:10px;margin:16px 0}.chip{background:#fff;border:1px solid #e3e7ed;border-radius:9px;padding:10px 14px;font-size:13px}.chip b{font-size:17px;margin-right:4px}.toolbar{background:#fff;border:1px solid #e1e6ed;border-radius:12px 12px 0 0;padding:14px;display:flex;gap:10px}.toolbar input,.toolbar select,.field input,.field select{border:1px solid #d9dfe8;border-radius:8px;padding:9px 10px;background:#fff;color:#253047}.toolbar input{width:260px}.table{background:#fff;border:1px solid #e1e6ed;border-top:0;border-radius:0 0 12px 12px;overflow:hidden}.row{display:grid;grid-template-columns:minmax(250px,1.7fr) 105px 80px 110px 110px 140px minmax(180px,1fr) 82px;align-items:center;gap:8px;padding:13px 15px;border-top:1px solid #edf0f4;font-size:13px}.row.header{background:#f8fafc;color:#677386;font-size:12px;border-top:0}.source-name{font-weight:600}.source-home{font-size:11px;color:#8390a1;margin-top:4px;overflow:hidden;text-overflow:ellipsis}.badge{display:inline-flex;align-items:center;justify-content:center;border-radius:99px;padding:4px 9px;font-size:12px;width:max-content}.ok,.published,.seed{background:#e9f8ef;color:#167347}.partial,.draft{background:#fff5df;color:#91620a}.error{background:#fdecec;color:#a53535}.unknown,.unconfigured{background:#eef1f5;color:#637083}.btn{border:1px solid #d7dde6;background:#fff;border-radius:8px;padding:8px 11px;cursor:pointer;color:#26344c}.btn:hover{background:#f4f7fb}.btn.primary{background:#2457d6;color:#fff;border-color:#2457d6}.btn.danger{color:#a53232}.expand{grid-column:1/-1;background:#f8fafc;border-radius:8px;padding:12px 14px;margin-top:5px}.endpoint{display:grid;grid-template-columns:140px 100px 1fr 90px;gap:10px;padding:8px 0;border-bottom:1px solid #e7ebf0}.endpoint:last-child{border:0}.empty{padding:70px;text-align:center;color:#7a8698}.drawer{position:fixed;inset:0;display:none;background:rgba(16,24,40,.35);z-index:20}.drawer.open{display:block}.panel{position:absolute;right:0;top:0;height:100%;width:640px;background:#fff;box-shadow:-12px 0 35px rgba(15,23,42,.16);overflow:auto;padding:24px 27px}.panel-head{display:flex;justify-content:space-between;align-items:center}.panel h2{margin:0}.section{border-top:1px solid #e7ebf0;margin-top:22px;padding-top:18px}.fields{display:grid;grid-template-columns:1fr 1fr;gap:12px}.field{display:flex;flex-direction:column;gap:6px;font-size:12px;color:#5f6b7d}.field.wide{grid-column:1/-1}.endpoint-card{border:1px solid #dfe5ed;border-radius:10px;padding:13px;margin:10px 0}.endpoint-grid{display:grid;grid-template-columns:140px 1fr 90px;gap:9px}.actions{display:flex;gap:9px;position:sticky;bottom:-24px;background:#fff;border-top:1px solid #e6eaf0;padding:15px 0 24px;margin-top:20px}.testbox{background:#f6f8fb;border-radius:10px;padding:13px;white-space:pre-wrap;font-size:12px;max-height:280px;overflow:auto}.history-item{display:flex;justify-content:space-between;gap:10px;padding:9px 0;border-bottom:1px solid #edf0f4}.toast{position:fixed;right:30px;bottom:30px;padding:12px 16px;background:#172033;color:#fff;border-radius:9px;display:none;z-index:40}.toast.show{display:block}.loading{opacity:.55;pointer-events:none}
</style>
</head>
<body>
<div class="shell">
  <aside class="side">
    <div class="brand"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M4 19V8l8-4 8 4v11M8 19v-5h8v5M3 19h18"/></svg>信息源管理中心</div>
    <h2>主题</h2><div id="topics"></div>
    <div class="side-foot">内部运维页面<br>${safeEmail}<br><span id="updated"></span></div>
  </aside>
  <main class="main">
    <div class="top"><div><h1 id="title">建筑信息源</h1><div class="muted">配置状态与真实运行健康分开显示；修改后须测试并发布。</div></div><button class="btn" id="reload">刷新状态</button></div>
    <div class="summary" id="summary"></div>
    <div class="toolbar">
      <input id="search" placeholder="搜索来源名称或域名">
      <select id="configFilter"><option value="">全部配置状态</option><option value="published">已发布</option><option value="draft">有草稿</option><option value="unconfigured">待配置</option><option value="seed">种子配置</option></select>
      <select id="runtimeFilter"><option value="">全部运行状态</option><option value="ok">正常</option><option value="partial">部分</option><option value="error">异常</option><option value="unknown">未知</option></select>
      <select id="regionFilter"><option value="">全部地区</option></select>
      <select id="levelFilter"><option value="">全部层级</option></select>
    </div>
    <div class="table"><div class="row header"><span>来源</span><span>地区</span><span>层级</span><span>采集栏目</span><span>配置状态</span><span>运行状态</span><span>当前问题</span><span>操作</span></div><div id="rows"></div></div>
  </main>
</div>
<div class="drawer" id="drawer"><div class="panel"><div class="panel-head"><div><div class="muted" id="breadcrumb"></div><h2 id="editorTitle"></h2></div><button class="btn" id="close">关闭</button></div><div id="editor"></div></div></div>
<div class="toast" id="toast"></div>
<script>
const api="/internal/api/sources"
const labels={building:"建筑",ai:"AI",finance:"财经",health:"健康"}
const kindLabels={policy:"政策文件",notice:"通知/公告",interpretation:"政策解读",news:"工作动态",standard:"标准规范",other:"其他"}
const state={topic:"building",model:null,expanded:new Set(),editing:null,test:null}
const esc=value=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]))
function toast(message){const element=document.getElementById("toast");element.textContent=message;element.classList.add("show");setTimeout(()=>element.classList.remove("show"),2800)}
async function request(method,body){const response=await fetch(api+(method?"":"?topic="+encodeURIComponent(state.topic)),{method:method||"GET",headers:method?{"content-type":"application/json"}:{},body:method?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.statusMessage||data.message||("HTTP "+response.status));return data}
async function load(){document.body.classList.add("loading");try{state.model=await request();document.getElementById("updated").textContent="更新于 "+new Date().toLocaleTimeString();render()}catch(error){toast(error.message)}finally{document.body.classList.remove("loading")}}
function badge(value){const name=String(value||"unknown").toLowerCase();const text={ok:"正常",partial:"部分",error:"异常",unknown:"未知",published:"已发布",draft:"有草稿",seed:"种子配置",unconfigured:"待配置"}[name]||name;return '<span class="badge '+esc(name)+'">'+esc(text)+"</span>"}
function filtered(){if(!state.model)return[];const query=document.getElementById("search").value.trim().toLowerCase();const config=document.getElementById("configFilter").value;const runtime=document.getElementById("runtimeFilter").value;const region=document.getElementById("regionFilter").value;const level=document.getElementById("levelFilter").value;return state.model.sources.filter(item=>{const source=item.draft||item.effective;return(!query||(source.name+" "+source.home).toLowerCase().includes(query))&&(!config||item.configStatus===config)&&(!runtime||String(item.runtime.status).toLowerCase()===runtime)&&(!region||source.region===region||source.city===region)&&(!level||source.level===level)})}
function render(){document.getElementById("title").textContent=(labels[state.topic]||state.topic)+"信息源";document.getElementById("topics").innerHTML=state.model.topics.map(topic=>'<button class="topic '+(topic.id===state.topic?"active":"")+'" data-topic="'+esc(topic.id)+'"><span>'+esc(topic.name)+(topic.enabled?"":" · 未启用")+"</span><small>"+topic.count+"</small></button>").join("");document.querySelectorAll("[data-topic]").forEach(button=>button.onclick=async()=>{state.topic=button.dataset.topic;state.expanded.clear();await load()});const sources=state.model.sources;const count=status=>sources.filter(item=>item.runtime.status===status).length;document.getElementById("summary").innerHTML='<div class="chip"><b>'+sources.length+'</b>来源</div><div class="chip"><b>'+count("ok")+'</b>正常</div><div class="chip"><b>'+count("partial")+'</b>部分</div><div class="chip"><b>'+count("error")+'</b>异常</div><div class="chip"><b>'+sources.filter(item=>item.configStatus==="unconfigured").length+"</b>待配置</div>";for(const [id,key] of [["regionFilter","region"],["levelFilter","level"]]){const element=document.getElementById(id);const old=element.value;const base=element.options[0].outerHTML;const values=[...new Set(sources.map(item=>(item.draft||item.effective)[key]).filter(Boolean))].sort();element.innerHTML=base+values.map(value=>"<option>"+esc(value)+"</option>").join("");element.value=old}renderRows()}
function renderRows(){const list=filtered();const root=document.getElementById("rows");if(!list.length){root.innerHTML='<div class="empty">没有符合条件的信息源</div>';return}root.innerHTML=list.map(item=>{const source=item.draft||item.effective;const endpoints=source.endpoints||[];const open=state.expanded.has(item.id);const message=item.runtime.message||(item.configStatus==="unconfigured"?"尚未确认实际采集栏目":"—");const details=open?'<div class="expand">'+(endpoints.length?endpoints.map(endpoint=>'<div class="endpoint"><b>'+esc(endpoint.name)+"</b><span>"+esc(kindLabels[endpoint.kind]||endpoint.kind)+"</span><span>"+esc(endpoint.url)+"</span>"+badge(endpoint.enabled?"ok":"unknown")+"</div>").join(""):"尚无明确栏目")+"</div>":"";return '<div class="row"><div><div class="source-name">'+esc(source.name)+'</div><div class="source-home">'+esc(source.home)+'</div></div><span>'+esc([source.region,source.city].filter(Boolean).join(" · ")||"全国")+'</span><span>'+esc(source.level)+'</span><button class="btn" data-expand="'+esc(item.id)+'">'+endpoints.filter(endpoint=>endpoint.enabled).length+" 个 "+(open?"收起":"展开")+"</button>"+badge(item.configStatus)+badge(item.runtime.status)+'<span title="'+esc(message)+'">'+esc(message.slice(0,45))+'</span><button class="btn primary" data-edit="'+esc(item.id)+'">编辑</button>'+details+"</div>"}).join("");document.querySelectorAll("[data-expand]").forEach(button=>button.onclick=()=>{state.expanded.has(button.dataset.expand)?state.expanded.delete(button.dataset.expand):state.expanded.add(button.dataset.expand);renderRows()});document.querySelectorAll("[data-edit]").forEach(button=>button.onclick=()=>openEditor(button.dataset.edit))}
function endpointCard(endpoint={}){return '<div class="endpoint-card" data-id="'+esc(endpoint.id||"")+'"><div class="endpoint-grid"><select data-kind>'+Object.entries(kindLabels).map(([key,value])=>'<option value="'+key+'" '+(endpoint.kind===key?"selected":"")+">"+value+"</option>").join("")+'</select><input data-name value="'+esc(endpoint.name||"")+'" placeholder="栏目名称"><label><input type="checkbox" data-enabled '+(endpoint.enabled!==false?"checked":"")+'> 启用</label><input style="grid-column:1/3" data-url value="'+esc(endpoint.url||"")+'" placeholder="https://官网/栏目页"><button class="btn danger" data-remove>删除</button></div></div>'}
function configFromForm(){const seed=state.editing;const get=id=>document.getElementById(id);return{...seed,id:seed.id,topic:seed.topic,name:get("f-name").value,home:get("f-home").value,group:get("f-group").value,level:get("f-level").value,region:get("f-region").value,city:get("f-city").value,priority:Number(get("f-priority").value),enabled:get("f-enabled").checked,collectionMode:get("f-mode").value,endpoints:[...document.querySelectorAll(".endpoint-card")].map((card,index)=>({id:card.dataset.id||seed.id+":endpoint:"+(index+1),name:card.querySelector("[data-name]").value,kind:card.querySelector("[data-kind]").value,url:card.querySelector("[data-url]").value,enabled:card.querySelector("[data-enabled]").checked}))}}
function openEditor(id){const item=state.model.sources.find(source=>source.id===id);const config=structuredClone(item.draft||item.effective);state.editing=config;state.test=null;document.getElementById("breadcrumb").textContent=(labels[config.topic]||config.topic)+" / "+config.id;document.getElementById("editorTitle").textContent=config.name;document.getElementById("editor").innerHTML='<div class="section fields"><label class="field wide">来源名称<input id="f-name" value="'+esc(config.name)+'"></label><label class="field wide">官网地址<input id="f-home" value="'+esc(config.home)+'"></label><label class="field">来源分组<input id="f-group" value="'+esc(config.group)+'"></label><label class="field">层级<input id="f-level" value="'+esc(config.level)+'"></label><label class="field">地区<input id="f-region" value="'+esc(config.region)+'"></label><label class="field">城市<input id="f-city" value="'+esc(config.city)+'"></label><label class="field">优先级<input id="f-priority" type="number" value="'+config.priority+'"></label><label class="field">采集模式<select id="f-mode"><option value="discover">发现候选（待配置）</option><option value="explicit">明确栏目</option><option value="feed">结构化信息流</option></select></label><label class="field"><span>来源状态</span><span><input id="f-enabled" type="checkbox" '+(config.enabled?"checked":"")+'> 启用</span></label></div><div class="section"><b>采集栏目</b><span class="muted">　明确栏目模式不会退回官网首页。</span><div id="endpointList">'+(config.endpoints||[]).map(endpointCard).join("")+'</div><button class="btn" id="addEndpoint">添加栏目</button></div><div class="section"><b>测试结果</b><div class="testbox" id="testbox">尚未测试当前草稿</div></div><div class="section"><b>版本历史</b><div>'+(item.history.length?item.history.map(history=>'<div class="history-item"><span>#'+history.revision+" · "+new Date(history.createdAt).toLocaleString()+" · "+esc(history.reason)+'</span><button class="btn" data-rollback="'+history.revision+'">回滚</button></div>').join(""):"尚无已发布版本")+'</div></div><div class="actions"><button class="btn" id="save">保存草稿</button><button class="btn" id="test">测试当前配置</button><button class="btn primary" id="publish">发布配置</button></div>';document.getElementById("f-mode").value=config.collectionMode;bindEditor();document.getElementById("drawer").classList.add("open")}
function bindRemoves(){document.querySelectorAll("[data-remove]").forEach(button=>button.onclick=()=>button.closest(".endpoint-card").remove())}
function bindEditor(){document.getElementById("addEndpoint").onclick=()=>{document.getElementById("endpointList").insertAdjacentHTML("beforeend",endpointCard({kind:"notice",enabled:true}));bindRemoves()};bindRemoves();document.getElementById("save").onclick=saveDraft;document.getElementById("test").onclick=testDraft;document.getElementById("publish").onclick=publishDraft;document.querySelectorAll("[data-rollback]").forEach(button=>button.onclick=()=>rollback(Number(button.dataset.rollback)))}
async function saveDraft(){try{const result=await request("POST",{action:"save-draft",topic:state.topic,sourceId:state.editing.id,config:configFromForm()});state.editing=result.config;state.test=null;toast("草稿已保存");await load();openEditor(state.editing.id)}catch(error){toast(error.message)}}
async function testDraft(){try{document.getElementById("testbox").textContent="正在按顺序读取官方页面…";const result=await request("POST",{action:"test",topic:state.topic,sourceId:state.editing.id,config:configFromForm()});state.editing=result.config;state.test=result;document.getElementById("testbox").textContent=JSON.stringify(result.result,null,2);toast(result.result.ok?"测试通过":"测试未通过")}catch(error){document.getElementById("testbox").textContent=error.message;toast(error.message)}}
async function publishDraft(){try{if(!state.test?.hash)throw new Error("请先测试当前配置");await request("POST",{action:"publish",topic:state.topic,sourceId:state.editing.id,hash:state.test.hash});toast("配置已发布");document.getElementById("drawer").classList.remove("open");await load()}catch(error){toast(error.message)}}
async function rollback(revision){if(!confirm("将该历史版本作为新版本重新发布？"))return;try{await request("POST",{action:"rollback",topic:state.topic,sourceId:state.editing.id,revision});toast("已回滚并生成新版本");document.getElementById("drawer").classList.remove("open");await load()}catch(error){toast(error.message)}}
document.getElementById("close").onclick=()=>document.getElementById("drawer").classList.remove("open")
document.getElementById("drawer").onclick=event=>{if(event.target.id==="drawer")event.currentTarget.classList.remove("open")}
document.getElementById("reload").onclick=load
for(const id of ["search","configFilter","runtimeFilter","regionFilter","levelFilter"])document.getElementById(id).addEventListener(id==="search"?"input":"change",renderRows)
load()
</script>
</body>
</html>`
}
