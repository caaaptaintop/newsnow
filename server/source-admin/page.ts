export function sourceAdminPage(email: string, nonce = "") {
  const safeEmail = email.replace(/[&<>"']/g, char => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[char]!)
  return `<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>信息源管理中心</title>
<style>
:root{font-family:Inter,"PingFang SC","Microsoft YaHei",sans-serif;color:#172033;background:#f4f6f9;font-synthesis:none}*{box-sizing:border-box}body{margin:0;min-width:760px}.shell{display:grid;grid-template-columns:230px 1fr;min-height:100vh}.side{background:#111827;color:#d7deea;padding:24px 16px;position:sticky;top:0;height:100vh}.brand{display:flex;align-items:center;gap:10px;color:#fff;font-weight:700;font-size:18px;padding:0 10px 24px}.brand svg{width:24px}.side h2{font-size:12px;color:#8792a5;font-weight:600;letter-spacing:.08em;margin:10px}.topic{width:100%;border:0;background:transparent;color:inherit;border-radius:9px;padding:12px 11px;margin:3px 0;text-align:left;display:flex;justify-content:space-between;cursor:pointer;font-size:14px}.topic:hover,.topic.active{background:#273449;color:#fff}.topic small{background:#3a4658;border-radius:12px;padding:2px 8px}.side-foot{position:absolute;bottom:20px;left:25px;right:20px;color:#9aa5b5;font-size:12px;line-height:1.6}.main{padding:28px 34px;min-width:0}.table{overflow-x:auto}.row{min-width:1080px}@media(max-width:900px){.shell{grid-template-columns:170px 1fr}.main{padding:18px}.panel{width:min(640px,100vw)}.toolbar{flex-wrap:wrap}}.top{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:20px}.top h1{margin:0 0 6px;font-size:25px}.muted{color:#6c7789;font-size:13px}.summary{display:flex;gap:10px;margin:16px 0}.chip{background:#fff;border:1px solid #e3e7ed;border-radius:9px;padding:10px 14px;font-size:13px}.chip b{font-size:17px;margin-right:4px}.toolbar{background:#fff;border:1px solid #e1e6ed;border-radius:12px 12px 0 0;padding:14px;display:flex;gap:10px}.toolbar input,.toolbar select,.field input,.field select{border:1px solid #d9dfe8;border-radius:8px;padding:9px 10px;background:#fff;color:#253047}.toolbar input{width:260px}.table{background:#fff;border:1px solid #e1e6ed;border-top:0;border-radius:0 0 12px 12px;overflow:hidden}.row{display:grid;grid-template-columns:minmax(250px,1.7fr) 105px 80px 110px 110px 140px minmax(180px,1fr) 82px;align-items:center;gap:8px;padding:13px 15px;border-top:1px solid #edf0f4;font-size:13px}.row.header{background:#f8fafc;color:#677386;font-size:12px;border-top:0}.source-name{font-weight:600}.source-home{font-size:11px;color:#8390a1;margin-top:4px;overflow:hidden;text-overflow:ellipsis}.badge{display:inline-flex;align-items:center;justify-content:center;border-radius:99px;padding:4px 9px;font-size:12px;width:max-content}.ok,.published,.seed{background:#e9f8ef;color:#167347}.partial,.draft{background:#fff5df;color:#91620a}.error{background:#fdecec;color:#a53535}.unknown,.unconfigured{background:#eef1f5;color:#637083}.btn{border:1px solid #d7dde6;background:#fff;border-radius:8px;padding:8px 11px;cursor:pointer;color:#26344c}.btn:disabled{opacity:.45;cursor:not-allowed}.btn:hover{background:#f4f7fb}.btn.primary{background:#2457d6;color:#fff;border-color:#2457d6}.btn.danger{color:#a53232}.expand{grid-column:1/-1;background:#f8fafc;border-radius:8px;padding:12px 14px;margin-top:5px}.endpoint{display:grid;grid-template-columns:140px 100px 1fr 90px;gap:10px;padding:8px 0;border-bottom:1px solid #e7ebf0}.endpoint:last-child{border:0}.empty{padding:70px;text-align:center;color:#7a8698}.drawer{position:fixed;inset:0;display:none;background:rgba(16,24,40,.35);z-index:20}.drawer.open{display:block}.panel{position:absolute;right:0;top:0;height:100%;width:640px;background:#fff;box-shadow:-12px 0 35px rgba(15,23,42,.16);overflow:auto;padding:24px 27px}.panel-head{display:flex;justify-content:space-between;align-items:center}.panel h2{margin:0}.section{border-top:1px solid #e7ebf0;margin-top:22px;padding-top:18px}.fields{display:grid;grid-template-columns:1fr 1fr;gap:12px}.field{display:flex;flex-direction:column;gap:6px;font-size:12px;color:#5f6b7d}.field.wide{grid-column:1/-1}.endpoint-card{border:1px solid #dfe5ed;border-radius:10px;padding:13px;margin:10px 0}.endpoint-grid{display:grid;grid-template-columns:140px 1fr 90px;gap:9px}.actions{display:flex;gap:9px;position:sticky;bottom:-24px;background:#fff;border-top:1px solid #e6eaf0;padding:15px 0 24px;margin-top:20px}.testbox{background:#f6f8fb;border-radius:10px;padding:13px;white-space:pre-wrap;font-size:12px;max-height:280px;overflow:auto}.history-item{display:flex;justify-content:space-between;gap:10px;padding:9px 0;border-bottom:1px solid #edf0f4}.toast{position:fixed;right:30px;bottom:30px;padding:12px 16px;background:#172033;color:#fff;border-radius:9px;display:none;z-index:40}.toast.show{display:block}.loading{opacity:.55;pointer-events:none}
</style>
</head>
<body>
<div class="shell">
  <aside class="side">
    <div class="brand"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M4 19V8l8-4 8 4v11M8 19v-5h8v5M3 19h18"/></svg>信息源管理中心</div>
    <h2>主题</h2><div id="topics"></div>
    <div class="side-foot">内部运维页面<br>${safeEmail}<br><span id="updated"></span></div>
  </aside>
  <main class="main">
    <div class="top"><div><h1 id="title">建筑信息源</h1><div class="muted">配置状态与真实运行健康分开显示；修改后须测试并发布。</div></div><button class="btn" id="reload">刷新状态</button></div>
    <div class="summary" id="summary"></div>
    <div class="toolbar">
      <input id="search" placeholder="搜索来源名称或域名">
      <select id="configFilter"><option value="">全部配置状态</option><option value="published">已发布</option><option value="draft">有草稿</option><option value="unconfigured">待配置</option><option value="seed">种子配置</option></select>
      <select id="runtimeFilter"><option value="">全部运行状态</option><option value="ok">正常</option><option value="partial">部分</option><option value="error">异常</option><option value="unknown">未知</option></select>
      <select id="regionFilter"><option value="">全部地区</option></select>
      <select id="levelFilter"><option value="">全部层级</option></select>
    </div>
    <div class="table"><div class="row header"><span>来源</span><span>地区</span><span>层级</span><span>采集栏目</span><span>配置状态</span><span>运行状态</span><span>当前问题</span><span>操作</span></div><div id="rows"></div></div>
  </main>
</div>
<div class="drawer" id="drawer"><div class="panel"><div class="panel-head"><div><div class="muted" id="breadcrumb"></div><h2 id="editorTitle"></h2></div><button class="btn" id="close">关闭</button></div><div id="editor"></div></div></div>
<div class="toast" id="toast"></div>
<script nonce="${nonce}">
const api="/internal/api/sources"
const labels={building:"建筑",ai:"AI",finance:"财经",health:"健康"}
const kindLabels={policy:"政策文件",notice:"通知/公告",interpretation:"政策解读",news:"工作动态",standard:"标准规范",other:"其他"}
const state={topic:'building',model:null,editing:null,base:null,test:null,dirty:false,busy:false,sequence:0,expanded:new Set()}
const el=id=>document.getElementById(id)
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))
const stamp=value=>value?new Date(value).toLocaleString('zh-CN'):'尚无记录'
let toastTimer
function toast(message){el('toast').textContent=message;el('toast').classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>el('toast').classList.remove('show'),6000)}
async function request(method,body,topic=state.topic){const response=await fetch(api+(method==='GET'?'?topic='+encodeURIComponent(topic):''),{method,credentials:'same-origin',cache:'no-store',headers:{'Content-Type':'application/json'},body:body?JSON.stringify(body):undefined,signal:AbortSignal.timeout(70000)});if(!String(response.headers.get('content-type')).includes('application/json'))throw new Error('会话可能已过期，请刷新页面重新认证');const result=await response.json();if(!response.ok)throw new Error(result.message||result.statusMessage||'请求失败');return result}
async function load(){const n=++state.sequence,topic=state.topic;try{const model=await request('GET',undefined,topic);if(n!==state.sequence||topic!==state.topic)return;state.model=model;el('updated').textContent='状态读取：'+stamp(Date.now());render()}catch(error){if(n!==state.sequence)return;el('rows').innerHTML='<div class="empty">'+esc(error.message)+'</div>';toast(error.message)}}
function badge(value){const label={ok:'正常',partial:'部分',error:'异常',unknown:'未知',published:'已发布',draft:'有草稿',seed:'种子配置',unconfigured:'待配置'}[value]||value;return '<span class="badge '+esc(value)+'">'+esc(label)+'</span>'}
function filtered(){if(!state.model)return[];const query=el('search').value.trim().toLowerCase();return state.model.sources.filter(item=>{const s=item.effective;return(!query||(s.name+' '+s.home).toLowerCase().includes(query))&&(!el('configFilter').value||item.configStatus===el('configFilter').value)&&(!el('runtimeFilter').value||item.runtime.status===el('runtimeFilter').value)&&(!el('regionFilter').value||s.region===el('regionFilter').value||s.city===el('regionFilter').value)&&(!el('levelFilter').value||s.level===el('levelFilter').value)})}
function render(){el('title').textContent=labels[state.topic]+'信息源';el('topics').innerHTML=state.model.topics.map(t=>'<button class="topic '+(t.id===state.topic?'active':'')+'" data-topic="'+esc(t.id)+'"><span>'+esc(t.name)+(t.enabled?'':' · 未启用')+'</span><small>'+t.count+'</small></button>').join('');document.querySelectorAll('[data-topic]').forEach(button=>button.onclick=()=>{if(state.busy||!closeEditor())return;state.topic=button.dataset.topic;state.expanded.clear();for(const id of ['search','configFilter','runtimeFilter','regionFilter','levelFilter'])el(id).value='';el('rows').innerHTML='<div class="empty">正在读取…</div>';load()});const sources=state.model.sources;el('summary').innerHTML=[['来源',sources.length],['正常',sources.filter(i=>i.runtime.status==='ok').length],['部分',sources.filter(i=>i.runtime.status==='partial').length],['异常',sources.filter(i=>i.runtime.status==='error').length],['待配置',sources.filter(i=>i.configStatus==='unconfigured').length]].map(([name,count])=>'<div class="chip"><b>'+count+'</b>'+name+'</div>').join('');for(const [id,key] of [['regionFilter','region'],['levelFilter','level']]){const select=el(id),old=select.value,first=select.options[0].outerHTML;const values=[...new Set(sources.flatMap(i=>key==='region'?[i.effective.region,i.effective.city]:[i.effective[key]]).filter(Boolean))].sort();select.innerHTML=first+values.map(v=>'<option>'+esc(v)+'</option>').join('');select.value=values.includes(old)?old:''}renderRows()}
function renderRows(){const list=filtered();el('rows').innerHTML=list.length?list.map(item=>{const s=item.effective,open=state.expanded.has(item.id),message=item.runtime.message||(item.configStatus==='unconfigured'?'尚未确认目标栏目':'—');const details=open?'<div class="expand"><div class="muted">最近检查：'+esc(stamp(item.runtime.updatedAt))+'；以下是当前生效栏目，不是未发布草稿。栏目启用不等于采集正常。</div>'+s.endpoints.map(e=>'<div class="endpoint"><b>'+esc(e.name)+'</b><span>'+esc(kindLabels[e.kind])+'</span><span>'+esc(e.url)+'</span><span>'+(e.enabled?'已启用':'已停用')+'</span></div>').join('')+(!s.endpoints.length?'尚无明确栏目':'')+'</div>':'';return '<div class="row"><div><div class="source-name">'+esc(s.name)+'</div><div class="source-home">'+esc(s.home)+'</div></div><span>'+esc([...new Set([s.region,s.city].filter(Boolean))].join(' · ')||'全国')+'</span><span>'+esc(s.level)+'</span><button class="btn" data-expand="'+esc(item.id)+'">'+s.endpoints.filter(e=>e.enabled).length+' 个 '+(open?'收起':'展开')+'</button>'+badge(item.configStatus)+badge(item.runtime.status)+'<span title="'+esc(message)+'">'+esc(message.slice(0,45))+'</span><button class="btn primary" data-edit="'+esc(item.id)+'">编辑</button>'+details+'</div>'}).join(''):'<div class="empty">没有符合条件的信息源</div>';document.querySelectorAll('[data-expand]').forEach(b=>b.onclick=()=>{state.expanded.has(b.dataset.expand)?state.expanded.delete(b.dataset.expand):state.expanded.add(b.dataset.expand);renderRows()});document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>openEditor(b.dataset.edit))}
function endpointCard(e={}){return '<div class="endpoint-card" data-id="'+esc(e.id||crypto.randomUUID())+'"><div class="endpoint-grid"><select data-kind aria-label="栏目类型">'+Object.entries(kindLabels).map(([k,v])=>'<option value="'+k+'" '+(e.kind===k?'selected':'')+'>'+v+'</option>').join('')+'</select><input data-name aria-label="栏目名称" value="'+esc(e.name||'')+'" placeholder="栏目名称"><label><input type="checkbox" data-enabled '+(e.enabled!==false?'checked':'')+'> 启用</label><input style="grid-column:1/3" data-url aria-label="栏目地址" value="'+esc(e.url||'')+'" placeholder="https://官网/栏目页"><button class="btn danger" data-remove>删除</button></div></div>'}
function configFromForm(){return{...state.editing,name:el('f-name').value,home:el('f-home').value,group:el('f-group').value,level:el('f-level').value,region:el('f-region').value,city:el('f-city').value,priority:Number(el('f-priority').value),enabled:el('f-enabled').checked,collectionMode:el('f-mode').value,endpoints:[...document.querySelectorAll('.endpoint-card')].map(c=>({id:c.dataset.id,name:c.querySelector('[data-name]').value,kind:c.querySelector('[data-kind]').value,url:c.querySelector('[data-url]').value,enabled:c.querySelector('[data-enabled]').checked}))}}
function markDirty(){state.dirty=true;state.test=null;el('testbox').textContent='配置已修改；此前测试已失效';syncPublish()}
function syncPublish(){if(el('publish'))el('publish').disabled=state.busy||state.dirty||!state.test?.result?.publishable}
function setBusy(value){state.busy=value;document.querySelectorAll('#editor input,#editor select,#editor button,#close,#reload,#topics button').forEach(item=>item.disabled=value);syncPublish()}
function closeEditor(){if(state.busy)return false;if(state.dirty&&!confirm('还有未保存的修改，确定关闭？'))return false;el('drawer').classList.remove('open');state.editing=null;state.dirty=false;state.test=null;return true}
function openEditor(id){const item=state.model.sources.find(i=>i.id===id);const c=structuredClone(item.draft||item.effective);state.editing=c;state.base={draftHash:item.draftHash,activeRevision:item.activeRevision};state.dirty=false;state.test=item.lastTest||null;el('breadcrumb').textContent=labels[c.topic]+' / '+c.id;el('editorTitle').textContent=c.name;const field=(id,label,value,wide=false)=>'<label class="field '+(wide?'wide':'')+'">'+label+'<input id="f-'+id+'" value="'+esc(value)+'"></label>';el('editor').innerHTML='<div class="section fields">'+field('name','来源名称',c.name,true)+field('home','官网地址',c.home,true)+field('group','来源分组',c.group)+field('level','层级',c.level)+field('region','地区',c.region)+field('city','城市',c.city)+field('priority','优先级',c.priority)+'<label class="field">采集模式<select id="f-mode">'+(c.newsnowId?'<option value="feed">结构化读取器（未启用主题仅维护草稿）</option>':'<option value="discover">发现候选（不可直接发布）</option><option value="explicit">明确栏目</option>')+'</select></label><label class="field"><span>来源状态</span><span><input id="f-enabled" type="checkbox" '+(c.enabled?'checked':'')+'> 启用（不改变主题总开关）</span></label></div><div class="section"><b>采集栏目</b><p class="muted">最多12个；明确栏目模式不会退回官网首页。请查看测试标题，确认页面选对。</p><div id="endpointList">'+c.endpoints.map(endpointCard).join('')+'</div><button class="btn" id="addEndpoint">添加栏目</button></div><div class="section"><b>测试结果</b><div class="testbox" id="testbox"></div></div><div class="section"><b>版本历史</b><p class="muted">恢复仅生成草稿，重新测试后才可发布。</p>'+item.history.map(h=>'<div class="history-item"><span>#'+h.revision+' · '+esc(stamp(h.createdAt))+'</span><button class="btn" data-rollback="'+h.revision+'">恢复为草稿</button></div>').join('')+'</div><div class="actions"><button class="btn" id="save">保存草稿</button><button class="btn" id="test">测试当前配置</button><button class="btn primary" id="publish" disabled>发布配置</button></div>';el('f-mode').value=c.collectionMode;el('editor').oninput=markDirty;el('editor').onchange=markDirty;el('addEndpoint').disabled=!!c.newsnowId;el('addEndpoint').onclick=()=>{if(document.querySelectorAll('.endpoint-card').length>=12){toast('最多12个栏目');return}el('endpointList').insertAdjacentHTML('beforeend',endpointCard({kind:'notice',enabled:true}));bindRemoves();markDirty()};bindRemoves();el('save').onclick=()=>editAction('save-draft');el('test').onclick=()=>editAction('test');el('publish').onclick=publishDraft;document.querySelectorAll('[data-rollback]').forEach(b=>b.onclick=()=>restore(Number(b.dataset.rollback)));renderTest();syncPublish();el('drawer').classList.add('open')}
function bindRemoves(){document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{b.closest('.endpoint-card').remove();markDirty()})}
function renderTest(){const t=state.test?.result;el('testbox').textContent=t?JSON.stringify(t,null,2):'尚未测试当前草稿';if(t?.candidates?.length){el('testbox').innerHTML='<div>'+esc(t.message)+'</div>'+t.candidates.map((c,index)=>'<p><button class="btn" data-candidate="'+index+'">添加候选</button> '+esc(c.name)+'<br>'+esc(c.url)+'</p>').join('');document.querySelectorAll('[data-candidate]').forEach(b=>b.onclick=()=>{const c=t.candidates[Number(b.dataset.candidate)];if(document.querySelectorAll('.endpoint-card').length>=12)return toast('最多12个栏目');if([...document.querySelectorAll('[data-url]')].some(i=>i.value===c.url))return toast('栏目已添加');el('endpointList').insertAdjacentHTML('beforeend',endpointCard({name:c.name,url:c.url,kind:'other',enabled:true}));el('f-mode').value='explicit';bindRemoves();markDirty()})}}
async function editAction(action){if(state.busy)return;const config=configFromForm();setBusy(true);if(action==='test')el('testbox').textContent='正在测试；不调用 AI，不发布文章…';try{const result=await request('POST',{action,topic:config.topic,sourceId:config.id,config,base:state.base});state.base={draftHash:result.hash,activeRevision:result.activeRevision};state.editing=result.config;state.dirty=false;state.test=action==='test'?result:null;const form=document.getElementById('editor');for(const [key,value] of Object.entries(result.config)){const input=el('f-'+key);if(input&&key!=='enabled'&&key!=='collectionMode')input.value=value}renderTest();toast(action==='test'?(result.result.publishable?'测试完成；确认标题样本后可发布':'测试完成，但当前配置不可发布'):'草稿已保存')}catch(error){state.test=null;el('testbox').textContent=error.message;toast(error.message)}finally{setBusy(false)}}
async function publishDraft(){if(state.busy)return;if(state.dirty||!state.test?.result?.publishable)return toast('请先测试当前配置');if(!confirm('确认测试中的标题来自你要监测的栏目，并发布供下一自然周期使用？'))return;setBusy(true);try{await request('POST',{action:'publish',topic:state.editing.topic,sourceId:state.editing.id,base:state.base,hash:state.test.hash,testedAt:state.test.testedAt});state.dirty=false;el('drawer').classList.remove('open');state.editing=null;toast('已发布配置；Mac实际采用情况需观察下一自然周期');await load()}catch(error){state.test=null;toast(error.message)}finally{setBusy(false)}}
async function restore(revision){if(state.busy)return;if(!confirm('仅恢复历史版本为草稿，不立即影响采集。继续？'))return;setBusy(true);try{const id=state.editing.id;await request('POST',{action:'rollback',topic:state.editing.topic,sourceId:id,revision,base:state.base});state.dirty=false;await load();openEditor(id);toast('历史版本已恢复为草稿；请重新测试')}catch(error){toast(error.message)}finally{setBusy(false)}}
el('close').onclick=closeEditor
el('drawer').onclick=event=>{if(event.target.id==='drawer')closeEditor()}
document.addEventListener('keydown',event=>{if(event.key==='Escape'&&state.editing)closeEditor()})
el('reload').onclick=()=>{if(closeEditor())load()}
for(const id of ['search','configFilter','runtimeFilter','regionFilter','levelFilter'])el(id).addEventListener(id==='search'?'input':'change',renderRows)
load()

</script>
</body>
</html>`
}
