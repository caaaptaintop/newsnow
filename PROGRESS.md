# Issue98：发布与验证

用户已确认后台预览，并将时刻明确为北京时间05:00、12:00、16:00。新增“立即采集并上线”沿用既有管理员Access与Mac签名operate能力；D1只新增独立任务表，不改文章及来源配置。单活跃任务唯一约束、原子claim与owner绑定finish防重复执行。Mac同一launchd每60秒仅轮询控制接口，指定小时内首次检查执行一次，失败需手动重试；整小时离线不会自动追补。

本地预览 http://127.0.0.1:4198/internal/sources 使用隔离SQLite，Mac执行为演示；已实际验证按钮排队、禁用和完成状态。生产尚未切换，发布前仍须固定候选、必要独立审查及相关Git/CI。验收覆盖任务重复请求、claim竞争、签名能力、Access/同源、报告丢失恢复、锁保护及时间段判定。既有完整正文、单栏目、PDF/下载不在此次修改范围。
