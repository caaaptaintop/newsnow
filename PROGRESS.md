# 当前阶段：Issue94 可操作预览已交付，待用户确认

2026-09-14 浏览器7条批注整改：卡片删除栏目/类型标签、采集栏目及证据说明，关注度移至日期左侧；隐藏前台信息源入口与来源筛选；类型单选并排除热点选题，旧URL清除来源条件并规范单类型。逐篇比对/提取说明仅本地验收工具，默认折叠；正式页面保留最近成功发布时间与刷新。真实Tabbit 1283/813宽度截图检查和类型筛选3→12篇交互通过。

正文增量：解析器取消8000字截断，分类器取消1200字截断；100000字安全上限保持，超限不静默截断发送。网页正文仅内存使用，附件正文不读取；单一contentType验证增加建筑热点选题拒绝。63项相关回归通过，含超过8000字末尾保留及类型数组拒绝。本次产品增量尚未发布、未真实调用AI；现有12篇候选仍为原分析，2篇旧截断不能视为全文重分析。原46项只绑定旧执行包，不覆盖本次产品增量；旧冻结计划保持不动，全文新候选尚待生成。生产清理/回填授权门仍保留。以下为上一阶段证据。

2026-09-14：复用现有工作区和已审候选，按修改→预览确认→发布推进。预览入口为 `http://127.0.0.1:4194/candidate`、`/baseline`、`/compare`（后两者同host/port）；仅本机、仅本地候选，未回填生产。详细交付与唯一当前观察见 `.local/body-94/preview/README.md` 和 `live-state.json`。

已完成：真实产品页面显示12篇正文候选和单栏目；逐篇对比可切换，展示摘要/类型/关注度变化及1200字截断边界；10个附件字段与来源配置保持。10组接口/数据检查通过；Tabbit已验证模式切换、单栏目、候选搜索命中/基线无结果、Word附件入口及文章选择。未作移动端或像素级截图验收；PDF展开/刷新等补充动作未完成，不计PASS。

已核整改后的 `.local/body-94/execution-independent-review.md`：本地46项PASS、生产BLOCKED；manifest `386f3f8fa2fb24c7bda9269d845b5d8706828cc7221ebced6d921663deeaa4e2` 的9/9文件已核验一致，本阶段未重跑46项，未修改冻结执行包。PR #96已合并，合并提交为 `d2ce57fd408cc7f42f29f680aedd9b58de2d5318`。

当前生产观察（北京时间2026-09-14 10:20:11）：公开version/feed/version均37/12，12篇仍title，逐字段匹配基线；worker最近10:13:04以error结束。真实result为229篇/306decisions/18附件更新，217篇不在当前12篇中，18附件目标全部缺失；真实merge报错，直接去除附件更新会误加入212篇当前不在线文章。本次result哈希 `9aaca9103f7b8303caf0e2ced2fb5e1a2871b49c0ec9df6a6d81b5fc56f8a5de`，仍不同于旧冻结输入。

更小清理候选已做零生产写入的内存验证：只裁剪列明的217旧article和18失效attachmentUpdate，保留306decisions；真实merge仍为原12篇，文章不变，217/217旧文章继续有完整本地key/title去重覆盖。精确对象清单已保存在当前观察中。本证据不等于真实下一周期、生产known或新执行器独立审查通过。

未完成与下一动作：用户确认预览；A旧残留清理与B现有12篇历史回填分别取得明确授权。A的执行候选须新输入绑定、双锁/CAS及并发新批次保护，内部独立审查只查增量；不再寻找旧result快照或恢复旧文章。B沿用适用的已审回填保护并核验真实生产协议。不得修改旧manifest伪装为已审新方案。现有正常调度未变，自动更新仍不健康，恢复后需实际周期成功证据。

变更范围：本阶段仅增加忽略的 `.local/body-94/preview/` 预览/只读观察文件并更新本文件与HANDOFF；没有修改产品源码、生产数据、锁、ledger/outbox、调度或来源配置，没有调用AI、提交、合并或部署。

## ARCHIVED：第三轮修订单历史记录（以下42项和待复审状态已被上方46项报告替代）

用户指定使用 agy CLI 完成本地补证；根据主代理第三轮检查反馈（`.local/body-94/agy-fix-feedback-3.md`），已逐项修正 execution 实现与测试，仅在本地隔离环境完成验证，未执行生产恢复与回填。固定候选与原审查基线见 `.local/body-94/independent-review.md`。

第三轮修正与交付要点：
1. 强制显式 dataDir 绑定与 CLI 优先前置校验：
   - 在 `cli.ts` 的 `backfill` 命令中增加 `--data-dir` 选项，在 `execute` 模式下优先强校验 `--data-dir`，若缺失立即报错退出，绝不在检查数据目录前提前尝试加载 `--adapter-module`；
   - `backfillPlan` 核心函数在 `execute` 模式下强制要求显式 `dataDir`，严禁缺省 fallback 到状态目录；
   - 增加真实 CLI 子进程隔离测试（测试 34、35），验证临时 `dataDir` 存在 `worker.lock` 时绝不调用 adapter 的任何接口，状态与备份零写入，预存锁安全保留。
2. 提取公共 verifyExportedArticles 函数并全面复用：
   - 提取公共函数 `verifyExportedArticles`，统一严格核验：版本为 37、篇数为 12、无正文持久化、ID 完整无重复、全公开字段匹配基线、包含真实隐藏分析字段；
   - 在首次全新发送分支、orphan 备份分支、prepared 状态恢复分支中统一调用复用，消除重复代码；
   - prepared 恢复分支重新从服务端导出当前文章并与已绑定的写前备份进行比对，若检测到静默字段变更（即便 revision 未推进）立即抛错阻断；
   - orphan 备份分支对当前线上导出和磁盘备份均调用 `verifyExportedArticles` 进行强核验，任何不一致或异常立即阻断。
3. 自动化隔离回归测试套件全面扩充：在 `runner.mts` 中补齐针对 feedback-3 的全部测试用例，共计 42 项详尽隔离回归、故障恢复与 CLI 子进程隔离测试 100% 通过（42 PASS，0 FAIL）。
4. 真实候选 CLI dry-run 验证：执行 `backfill --mode dry-run`，返回退出码 0，核验通过且绝对零写零加锁。
5. 清单与验收文档：更新 `manifest.json`（最新 SHA-256 `6cef796f9005ada2b569d04276c6786fb72e19c2ccb96f0c6a5681a1ad5ee890`）、`RESULT.md` 与 `README.md`。

测试边界与生产状态澄清：
- 进程中断测试通过持久化中间状态模拟，未向运行进程发送 SIGKILL 进行真实物理崩溃注入；
- 完整下一轮真实 known 集合仍未验证，历史硬删除文章在生产线上是否存在墓碑或去重泄漏仍作为已知条件性风险记录，不声称已关闭这一缺口；
- 线上生产 revision 37 / totalPublished 12 仅为上次只读观察记录，本轮未作线上复核，严禁声称为当前线上状态；
- 当前工作区存在 `HANDOFF.md` 与 `PROGRESS.md` 的进度修改，未触碰任何产品代码；
- 本执行包未获生产放行，当前任务不作为最终独立审查者。下一动作是向主代理汇报第三轮修订单交付事实，由未参与实现的独立任务进行高级独立复审。

### 2026-09-14 预览文案补充调整
- 删除侧栏“二级栏目”、单栏目归属说明及页尾免责声明；主标题按当前栏目显示，默认“全部信息”。
- Tabbit 813×659 实际显示及智能建造→好房子→全部信息切换通过；组件 ESLint 0 error（保留原有1 warning），diff check 通过。仅本地待确认，未发布；完整正文候选重分析与生产处理边界不变。

### 2026-09-14 完整正文候选已生成并接入预览
- 当前候选 `.local/body-94/full-body-20260914/reanalysis-plan.json` SHA256 `66e53993344c0ecea82f38ef0a4a0b71c5101382f84ec4d0b2751374bd9a346c`；既有 gpt-5.6-luna 订阅CLI真实运行，12/12 body/keep=true、无截断、附件正文未读取，公开基线前后37/12。
- preview/data.ts绑定新候选，旧计划/执行包不改。12卡片、7栏目唯一归属由加载断言保护，10附件和全部非分析字段不变；Tabbit813显示、城市更新6篇通过；63项相关产品测试通过。
- 当前仍待用户最终预览确认，未提交部署；用户要求按全部顺序完成，不把该指令冒充尚未展示的新候选验收。清理与历史回填具体确认门继续保留，原46项证据不覆盖新候选绑定与新清理路径。

### 2026-09-14 发布授权
用户确认当前预览先跑通流程，授权同范围正常发布和验证。当前进入发布；清理与历史回填保持独立边界。正文解析文件为通过正常提交检查做等价lint整理，不更改网络/附件安全边界。
