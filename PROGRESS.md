# 当前阶段：附件预览回源与预算（Issue #88）

云端隔离探针确认住建部530/1016及DNS SERVFAIL，本机同文件完整返回。候选增加仅对此故障启用的签名Mac备用读取，沿用索引授权、原子预算和短期Cache API；缺配置失败关闭。Mac只支持住建部实测附件路径，不扩展通用代理。

免费账户已核对；隔离D1真实预留结算共15写行，按16保守计算3000请求=48k写行。实际仍取既有后台2000较小值，不新增500/512MiB限制、不宣称实时账户余额、不启用R2。临时探针Worker和临时D1已删除，生产站点/数据库/调度未改。

本地测试覆盖认证、重放、路径跳转和选择性回退；真实签名Mac处理器返回DOCX200/61634字节。隔离Cloudflare→Quick Tunnel→Mac链路已实测200且哈希一致、缓存7ms、停Mac冷缓存503/热缓存200；真实DOCX在Tabbit展示标题和表格。浏览器访问临时workers.dev端点超时，云端传输与渲染分别取证。尚未提交、推送或生产部署，不宣称线上恢复。

下一步固定候选；缓存和新增认证边界按现行2.6节完成高级独立审查后才可合并。详细证据与部署方案见docs/attachment-preview.md、.local/attachment-88/。

2026-09-13：按用户要求PDF链接直接官方新标签；非PDF全站最多5个加载请求，Mac5、云端实例2、同IP2保护。满额提示稍后重试/官网，无自动队列。87项定向测试及构建通过，包含第6个拒绝及成功/失败释放、PDF直链渲染；证据.local/attachment-88/tests-concurrency.txt与build-concurrency.txt。尚未发布。

提交前补验：修正新增服务import排序；移除与Node无JSX配置冲突的组件测试，采用Tabbit实际点击取证。87项通过、改动文件无新增类型诊断。PDF点击创建新标签、源链接准确、本站转发0次/弹窗0个；合成域名新标签网络失败，未冒称官方渲染通过。历史React ESLint缺失规则仍阻断提交检查，未跳过hook、未提交/推送/部署。

2026-09-13 本次提交快照：用户仅授权 Issue #88 一次性绕过历史 ESLint 配置阻断以建立固定本地 commit，不认定lint通过，不改变其他门槛。独立待修问题及原因记录于docs/attachment-preview.md。形成Head即停止，等待独立审查，不推送、合并或部署。
